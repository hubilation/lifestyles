type User {
  id: ID! @unique
  email: String @unique
  name: String!
  password: String!
  resetToken: String
  resetTokenExpiry: Float
  permissions: [Permission]
}

enum Permission {
  ADMIN
  USER
  PERMISSIONUPDATE
  TRAINER
}